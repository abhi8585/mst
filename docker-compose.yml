version: '3'
services:
  dp-admin-web-app:
    restart: always
    env_file: .env
    build: .
    ports:
      - "8001:8001"
    networks:
      - db_network
      - web_network
  nginx:
    restart: always
    image: "nginx:latest"
    ports:
      - "80:80"
    volumes:
      - ./nginx:/etc/nginx/conf.d
    networks:
      - web_network
    depends_on: 
      - dp-admin-web-app
networks:
  db_network:
    driver: bridge
  web_network:
    driver: bridge
